<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.vaadin.reverseproxy</groupId>
    <artifactId>vaadin-reverseproxy-itests</artifactId>
    <version>1.0-SNAPSHOT</version>
    <packaging>jar</packaging>

    <properties>
        <maven.compiler.release>21</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <compiler.plugin.version>3.16.0</compiler.plugin.version>
        <resources.plugin.version>3.5.0</resources.plugin.version>
        <playwright.version>1.62.0</playwright.version>
        <junit.version>6.1.3</junit.version>
        <failsafe.version>3.6.0</failsafe.version>
        <app.base.url>http://localhost:8080/</app.base.url>
        <!-- Comma-separated push transports the transport-parameterized tests
             run through. The *-sse scenarios override this with
             SERVER_SENT_EVENTS, since their proxy cannot upgrade. -->
        <it.push.transports>WEBSOCKET_XHR,WEBSOCKET</it.push.transports>
        <!-- Whether the scenario proxy can upgrade a connection. The *-sse
             scenarios set it to false, which skips the Hilla push assertions:
             Hilla FluxConnection is WebSocket-only. -->
        <it.websocket>true</it.websocket>
    </properties>

    <dependencies>
        <dependency>
            <groupId>com.microsoft.playwright</groupId>
            <artifactId>playwright</artifactId>
            <version>${playwright.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>${junit.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-params</artifactId>
            <version>${junit.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>${compiler.plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-resources-plugin</artifactId>
                    <version>${resources.plugin.version}</version>
                </plugin>
            </plugins>
        </pluginManagement>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-failsafe-plugin</artifactId>
                <version>${failsafe.version}</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>integration-test</goal>
                            <goal>verify</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <trimStackTrace>false</trimStackTrace>
                    <enableAssertions>true</enableAssertions>
                    <systemPropertyVariables>
                        <app.base.url>${app.base.url}</app.base.url>
                        <it.push.transports>${it.push.transports}</it.push.transports>
                        <it.websocket>${it.websocket}</it.websocket>
                    </systemPropertyVariables>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
