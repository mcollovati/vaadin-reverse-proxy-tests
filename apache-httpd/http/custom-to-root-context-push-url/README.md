<!-- AUTOGENERATED by scripts/gen-readmes.sh — edit scenarios.tsv -->
# Apache HTTPD (HTTP) — custom-to-root-context-push-url

custom-to-root-context with VAADIN_PUSH_URL=/VAADIN/push; adds a ws:// proxy block for /app/VAADIN/push that maps to the backend's /VAADIN/push.

Proxy URL on `http://localhost:9090`: `/app/`

## Configuration

See [docker-compose.yml](./docker-compose.yml) for service definitions.

Apache HTTPD config (`vaadin.conf`):

```apache
<Location /app/>
    ProxyPass                  "http://vaadin:8080/"
    ProxyPassReverse           "http://vaadin:8080/"
    ProxyPassReverseCookiePath "/" "/app"
</Location>

<Location /app/VAADIN/push>
    ProxyPass "ws://vaadin:8080/VAADIN/push"
</Location>

<Location /app/HILLA/push>
    ProxyPass "ws://vaadin:8080/HILLA/push"
</Location>
```
